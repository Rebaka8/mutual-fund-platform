# No external API key required by default. The app uses local fallbacks and synthetic NAV data.
# If you want to enable live API data in future, uncomment and set the variable below.
# VITE_API_NINJAS_KEY=your_api_key_here
